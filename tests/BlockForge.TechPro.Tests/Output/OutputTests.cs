using COMP_3951_BlockForge_TechPro;

/// <summary>
/// BlockForge ProjectFileManagerTests 
/// Author: Angus Grewal
/// Date: Apr 1 2026
/// Source: Self-written, with AI coaching. All code submitted is human written, based on ChatGPT guidance.
/// </summary>
namespace BlockForge.TechPro.Tests;

[TestClass]
public class OutputTests
{
    [TestMethod]
    public void Generate_OutputPseudoCode()
    {
        // To-do, set up a project, then feed it into the PseudoCode generator.
        // Then, verify that the file was created.
        // Fails until I'm done writing PseudoCodeGenerator.
        Assert.Fail("Pending implementation of PseudoCodeGenerator");
    }

    [TestMethod]
    public void Compare_PseudoCode()
    {
        // To-do, after the PseudoCode generator has created a .txt file, compare the two.
        // Use the Output Pseudocode.txt to compare against.
        // Fails until I'm done writing PseudoCodeGenerator.
        Assert.Fail("Pending implementation of PseudoCodeGenerator");
    }
}
